"use client";

import { Suspense } from "react";

const PaymentSuccessPage = () => {
  return (
    <Suspense>
      <PaymentSuccessPage />
    </Suspense>
  );
};

export default PaymentSuccessPage;
